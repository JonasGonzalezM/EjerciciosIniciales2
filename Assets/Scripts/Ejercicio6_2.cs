using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // a es False||False||False por lo que el valor es False
        // b es False || False por lo que el valor final es False 
        // c es True && True por lo que el valor es True
        // d es Flase || True por lo que el valor final es True
        // e es True || False por lo que el valor final es True
        // f es False && True por lo que el valor final es False
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
