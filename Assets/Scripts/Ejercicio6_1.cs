using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_1 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // a es False||True pero el resultado es True.
        // c es True||False pero el resultado es True.
        // e es True||True y el resultado es True.
        // b es True && False por lo que el resultado es False
        // d es True && False por lo que el valor es False
        // f es True || True y el valor final es True.
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
